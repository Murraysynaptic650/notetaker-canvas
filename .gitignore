# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Vite scratch files
vite.config.ts.timestamp-*.mjs
.vite/

# Test output
coverage/

# Editor / OS
.DS_Store
.idea/
*.swp

# Local env / secrets — API keys belong in the environment, never in the repo
.env
.env.local
.env.*.local

# The bridge's launch script holds a live subscription token. Only the
# .example is tracked; a real one leaked here once and had to be scrubbed
# from history, so keep it ignored.
claude-bridge/start_command

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json
